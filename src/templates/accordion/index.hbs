{{#extend "layout"}}
  {{#replace "bodyId"}}accordion-page{{/replace}}
  {{#replace "main"}}

    <section id="hero">
      <div>
        <h1>Accordion</h1>
      </div>
    </section>

  {{/replace}}
{{/extend}}
