{{#extend "layout"}}
  {{#replace "bodyId"}}css-page{{/replace}}
  {{#replace "main"}}

    <section id="title">
      <div>
        <h1>CSS</h1>
      </div>
    </section>

  {{/replace}}
{{/extend}}
