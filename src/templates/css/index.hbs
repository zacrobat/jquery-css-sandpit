{{#extend "layout"}}
  {{#replace "bodyId"}}css-page{{/replace}}
  {{#replace "main"}}

    <section id="hero">
      <div>
        <h1>CSS</h1>
      </div>
    </section>

  {{/replace}}
{{/extend}}
