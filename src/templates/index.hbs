{{#extend "layout"}}
  {{#replace "bodyId"}}landing-page{{/replace}}
  {{#replace "main"}}

    <section id="hero">
      <div>
        <h1>Home</h1>
        <p>this website</p>
      </div>
    </section>

  {{/replace}}
{{/extend}}
